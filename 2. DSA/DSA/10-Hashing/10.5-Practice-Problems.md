# 10.5 Hashing — Practice Problems

A curated set of LeetCode problems organized by pattern. Solve them in order — each builds on the previous pattern.

## Table of Contents
1. [Easy Problems](#easy-problems)
2. [Medium Problems](#medium-problems)
3. [Hard Problems](#hard-problems)
4. [Pattern Cheat Sheet](#pattern-cheat-sheet)
5. [Solutions Walkthrough](#solutions-walkthrough)

---

## Easy Problems

| # | Problem | LC # | Key Pattern | Importance |
|---|---------|------|-------------|------------|
| 1 | Two Sum | 1 | Map complement → index | ⭐⭐⭐⭐⭐ |
| 2 | Contains Duplicate | 217 | HashSet add returns false | ⭐⭐⭐⭐⭐ |
| 3 | Valid Anagram | 242 | Frequency map | ⭐⭐⭐⭐ |
| 4 | Intersection of Two Arrays | 349 | Two HashSets | ⭐⭐⭐ |
| 5 | Missing Number | 268 | Set lookup | ⭐⭐⭐ |
| 6 | Single Number | 136 | XOR or Set toggle | ⭐⭐⭐⭐ |
| 7 | Isomorphic Strings | 205 | Two-way char map | ⭐⭐⭐ |
| 8 | Word Pattern | 290 | Bijection check | ⭐⭐⭐ |
| 9 | Happy Number | 202 | Cycle via Set | ⭐⭐⭐ |
| 10 | Roman to Integer | 13 | Symbol map | ⭐⭐ |

---

## Medium Problems

| # | Problem | LC # | Key Pattern | Importance |
|---|---------|------|-------------|------------|
| 1 | Group Anagrams | 49 | Sort key → group map | ⭐⭐⭐⭐⭐ |
| 2 | Top K Frequent Elements | 347 | Frequency map + heap | ⭐⭐⭐⭐⭐ |
| 3 | Longest Consecutive Sequence | 128 | Set + start-of-sequence | ⭐⭐⭐⭐⭐ |
| 4 | Subarray Sum Equals K | 560 | Prefix sum + map | ⭐⭐⭐⭐⭐ |
| 5 | Longest Substring Without Repeating | 3 | Sliding window + map | ⭐⭐⭐⭐⭐ |
| 6 | 4Sum II | 454 | Two-pass complement map | ⭐⭐⭐⭐ |
| 7 | Contiguous Array | 525 | Prefix sum (0→-1) + map | ⭐⭐⭐⭐ |
| 8 | Find All Anagrams in String | 438 | Fixed-window freq map | ⭐⭐⭐⭐ |
| 9 | Minimum Window Substring | 76 | Sliding window + freq | ⭐⭐⭐⭐ |
| 10 | LRU Cache | 146 | HashMap + Doubly LL | ⭐⭐⭐⭐ |
| 11 | Repeated DNA Sequences | 187 | Rolling hash / substring set | ⭐⭐⭐ |
| 12 | Longest Palindrome by Concatenating | 5 | Frequency parity | ⭐⭐⭐ |
| 13 | Equal Row and Column Pairs | 2352 | Row hash as key | ⭐⭐⭐ |

---

## Hard Problems

| # | Problem | LC # | Key Pattern | Importance |
|---|---------|------|-------------|------------|
| 1 | Longest Duplicate Substring | 1044 | Binary search + rolling hash | ⭐⭐⭐⭐ |
| 2 | Substring with Concatenation of All Words | 30 | Window + word freq map | ⭐⭐⭐ |
| 3 | Maximum Frequency Stack | 895 | Map of freq + map of stack | ⭐⭐⭐ |
| 4 | All O(1) Data Structure | 432 | HashMap + DLL | ⭐⭐⭐ |

---

## Pattern Cheat Sheet

### Pattern 1: Frequency Map

```java
// Template
Map<T, Integer> freq = new HashMap<>();
for (T item : items) freq.merge(item, 1, Integer::sum);

// Problem: Valid Anagram (LC 242)
public boolean isAnagram(String s, String t) {
    if (s.length() != t.length()) return false;
    int[] count = new int[26];
    for (char c : s.toCharArray()) count[c - 'a']++;
    for (char c : t.toCharArray()) count[c - 'a']--;
    for (int x : count) if (x != 0) return false;
    return true;
}
// Time: O(n) | Space: O(1) — array beats HashMap here
```

### Pattern 2: One-Pass Complement Map

```java
// Template: find pair summing to target
Map<Integer, Integer> seen = new HashMap<>();
for (int i = 0; i < nums.length; i++) {
    int complement = target - nums[i];
    if (seen.containsKey(complement)) return new int[]{seen.get(complement), i};
    seen.put(nums[i], i);
}

// Problem: Two Sum (LC 1)
// Same code as above — classic template
```

### Pattern 3: Prefix Sum + HashMap

```java
// Template: count subarrays with sum == k
Map<Integer, Integer> prefixCount = new HashMap<>();
prefixCount.put(0, 1);
int sum = 0, count = 0;
for (int num : nums) {
    sum += num;
    count += prefixCount.getOrDefault(sum - k, 0);
    prefixCount.merge(sum, 1, Integer::sum);
}

// Problem: Subarray Sum Equals K (LC 560), Contiguous Array (LC 525)
// For LC 525: treat 0 as -1, find subarray with sum == 0
```

### Pattern 4: Grouping by Canonical Key

```java
// Template: group items by normalized key
Map<String, List<T>> groups = new HashMap<>();
for (T item : items) {
    String key = normalize(item);  // sort, count, etc.
    groups.computeIfAbsent(key, k -> new ArrayList<>()).add(item);
}

// Problem: Group Anagrams (LC 49)
// normalize = sort chars → key string

// Problem: Find All Anagrams (LC 438)
// fixed-window freq comparison
```

### Pattern 5: Set-Based Sequence

```java
// Template: find sequences efficiently
Set<Integer> numSet = new HashSet<>();
for (int n : nums) numSet.add(n);

for (int n : numSet) {
    if (!numSet.contains(n - 1)) {  // start of sequence
        // extend sequence forward
    }
}

// Problem: Longest Consecutive Sequence (LC 128)
```

### Pattern 6: Sliding Window + Map

```java
// Template: longest window with constraint
Map<Character, Integer> window = new HashMap<>();
int left = 0, result = 0;
for (int right = 0; right < s.length(); right++) {
    window.merge(s.charAt(right), 1, Integer::sum);
    while (/* window violates constraint */) {
        char leftChar = s.charAt(left++);
        window.merge(leftChar, -1, Integer::sum);
        if (window.get(leftChar) == 0) window.remove(leftChar);
    }
    result = Math.max(result, right - left + 1);
}
```

---

## Solutions Walkthrough

### Group Anagrams (LC 49) ⭐⭐⭐⭐⭐

**Approach:** Sort each string → same anagrams have same sorted key.

```java
public List<List<String>> groupAnagrams(String[] strs) {
    Map<String, List<String>> map = new HashMap<>();
    for (String s : strs) {
        char[] arr = s.toCharArray();
        Arrays.sort(arr);
        String key = new String(arr);
        map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
    }
    return new ArrayList<>(map.values());
}
// Time: O(n * k log k) | Space: O(nk)

// Optimization: use char count as key to avoid O(k log k) sort
private String countKey(String s) {
    int[] count = new int[26];
    for (char c : s.toCharArray()) count[c - 'a']++;
    return Arrays.toString(count);
}
// Time: O(n * k) | Space: O(nk)
```

---

### Subarray Sum Equals K (LC 560) ⭐⭐⭐⭐⭐

**Key Insight:** `sum[i..j] = prefixSum[j] - prefixSum[i-1] == k`  
→ `prefixSum[i-1] == prefixSum[j] - k`

```java
public int subarraySum(int[] nums, int k) {
    Map<Integer, Integer> map = new HashMap<>();
    map.put(0, 1);  // empty prefix
    int sum = 0, count = 0;
    for (int num : nums) {
        sum += num;
        count += map.getOrDefault(sum - k, 0);
        map.merge(sum, 1, Integer::sum);
    }
    return count;
}
// Time: O(n) | Space: O(n)
```

---

### Top K Frequent Elements (LC 347) ⭐⭐⭐⭐⭐

**Three approaches:**

```java
// Approach 1: Min-heap — O(n log k)
public int[] topKFrequent(int[] nums, int k) {
    Map<Integer, Integer> freq = new HashMap<>();
    for (int n : nums) freq.merge(n, 1, Integer::sum);

    PriorityQueue<Integer> minHeap = new PriorityQueue<>(
        Comparator.comparingInt(freq::get)
    );
    for (int key : freq.keySet()) {
        minHeap.offer(key);
        if (minHeap.size() > k) minHeap.poll();
    }

    int[] res = new int[k];
    for (int i = k - 1; i >= 0; i--) res[i] = minHeap.poll();
    return res;
}

// Approach 2: Bucket sort — O(n)
public int[] topKFrequentBucket(int[] nums, int k) {
    Map<Integer, Integer> freq = new HashMap<>();
    for (int n : nums) freq.merge(n, 1, Integer::sum);

    @SuppressWarnings("unchecked")
    List<Integer>[] buckets = new List[nums.length + 1];
    freq.forEach((num, f) -> {
        if (buckets[f] == null) buckets[f] = new ArrayList<>();
        buckets[f].add(num);
    });

    int[] res = new int[k];
    int idx = 0;
    for (int i = buckets.length - 1; i >= 0 && idx < k; i--) {
        if (buckets[i] != null)
            for (int n : buckets[i])
                if (idx < k) res[idx++] = n;
    }
    return res;
}
```

---

### Minimum Window Substring (LC 76) ⭐⭐⭐⭐

```java
public String minWindow(String s, String t) {
    Map<Character, Integer> need = new HashMap<>();
    for (char c : t.toCharArray()) need.merge(c, 1, Integer::sum);

    int left = 0, formed = 0, required = need.size();
    int minLen = Integer.MAX_VALUE, minLeft = 0;
    Map<Character, Integer> window = new HashMap<>();

    for (int right = 0; right < s.length(); right++) {
        char rc = s.charAt(right);
        window.merge(rc, 1, Integer::sum);
        if (need.containsKey(rc) && window.get(rc).equals(need.get(rc))) {
            formed++;
        }

        while (formed == required) {
            if (right - left + 1 < minLen) {
                minLen = right - left + 1;
                minLeft = left;
            }
            char lc = s.charAt(left++);
            window.merge(lc, -1, Integer::sum);
            if (need.containsKey(lc) && window.get(lc) < need.get(lc)) {
                formed--;
            }
        }
    }
    return minLen == Integer.MAX_VALUE ? "" : s.substring(minLeft, minLeft + minLen);
}
// Time: O(|s| + |t|) | Space: O(|s| + |t|)
```

---

### LRU Cache (LC 146) ⭐⭐⭐⭐

```java
public class LRUCache {
    private final int capacity;
    private final Map<Integer, Node> map;
    private final Node head, tail;  // dummy sentinels

    public LRUCache(int capacity) {
        this.capacity = capacity;
        map = new HashMap<>();
        head = new Node(0, 0);
        tail = new Node(0, 0);
        head.next = tail;
        tail.prev = head;
    }

    public int get(int key) {
        if (!map.containsKey(key)) return -1;
        Node node = map.get(key);
        moveToFront(node);
        return node.val;
    }

    public void put(int key, int value) {
        if (map.containsKey(key)) {
            Node node = map.get(key);
            node.val = value;
            moveToFront(node);
        } else {
            Node node = new Node(key, value);
            map.put(key, node);
            addToFront(node);
            if (map.size() > capacity) {
                Node lru = tail.prev;
                remove(lru);
                map.remove(lru.key);
            }
        }
    }

    private void addToFront(Node n) {
        n.next = head.next; n.prev = head;
        head.next.prev = n; head.next = n;
    }
    private void remove(Node n) {
        n.prev.next = n.next; n.next.prev = n.prev;
    }
    private void moveToFront(Node n) {
        remove(n); addToFront(n);
    }

    private static class Node {
        int key, val;
        Node prev, next;
        Node(int key, int val) { this.key = key; this.val = val; }
    }
}
// Time: O(1) all ops | Space: O(capacity)
```

---

## Key Takeaways

1. **Two Sum pattern** is everywhere — one-pass map storing complement → index.
2. **Prefix sum + HashMap** solves most subarray sum/count problems in O(n).
3. **Group by canonical key** (`computeIfAbsent`) handles anagram-type grouping.
4. **Bucket sort** beats heap for top-K frequency when total count ≤ n.
5. **Sliding window + HashMap** tracks "at most K distinct" or "minimum covering window".
6. **LRU Cache** = HashMap (O(1) key lookup) + Doubly Linked List (O(1) eviction).

**Previous**: [10.4 Hashing Techniques](./10.4-Hashing-Techniques.md) | **Next**: [11.1 Binary Tree Basics](../11-Trees/11.1-Binary-Tree-Basics.md)
